$(document).ready(() => {

    // Click Events

    $("#collect-contact").on("click", () => {
        generateExpModal();
    });

    let generateExpModal = () => {
        $("#survey").append("html", "<div class='question';> </div>");
        
    };

    $("#collect-experience").on("click", () => {

    });


    $("#collect-lifestyle").on("click", () => {

    });

    $("#submit-form").on("click", () => {

        // Collect full name, last name, and email.
        firstName = $("#first-name");
        lastName = $("#last-name");
        email = $("#email");

        // Collect experience value.

        // Collect lifestyle range value. 

        // Collect competition response
    
        // collect textarea input
    
        });

    // Load Modals
        let generateExpModal = () => {
            $("#survey").append("html", "<div class='question'> <h1> Have you ever had an online coach before? </h1> </div>");
            
        };

});
